# The Monopoly Game blog

This directory contains the GitHub Pages site for the project.

To publish it, configure GitHub Pages to deploy from the `main` branch and
the `/docs` folder. The site will then be available at the repository's Pages
URL.
